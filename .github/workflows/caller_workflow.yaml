name: hello
on: [workflow_dispatch, push]
jobs:
  call-workflow-passing-data:
    uses: Internal-Actions/public-cmw-called/.github/workflows/called_workflow.yaml@main
    with:
      username: mona-public
